describe('writing some tests', () => {
    it('i am testing karma', () => {
        const a = 10, b = 15;
        const answer = a + b;
        expect(answer).toBe(25);
    })
})

describe('using the snippets', () => {
    it('is easy', () => {

    });
});